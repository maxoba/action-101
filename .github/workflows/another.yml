name: checkout 
on:
    push:
      branches:
        - main 
    
      
jobs:
    terraform:
      runs-on: ubuntu-latest
      steps:
       - name: terraform
         run: echo "running terraform commands"
       - name: checkout
         uses: actions/checkout@v4
       - name: list
         run: ls
              
